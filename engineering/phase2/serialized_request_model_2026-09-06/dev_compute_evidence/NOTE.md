# Evidence note

Non-authoritative development-compute evidence (headless run per
/home/chromikey/AI/tools/dev_compute/README.md; no GUI). Request mci-dev-277d0d84ec7e,
one falsification worker (openai/gpt-oss-20b, fast_reviewer), panel COMPLETE.

The repository copy of the worker response has trailing whitespace (markdown hard
line breaks) stripped so the tree passes `git diff --check`. The unmodified raw file is
in the Operator handoff archive; its SHA-256 is:

    c752a1be31897dc51983e88762d8fe51cae53e21be8961afd0bd299a2ca0361b

No credential is present in any file here; `credential_source` is a location
description only.
