RESEARCH / PROPOSED DESIGN — NOT ADOPTED. pass-2 results (debug profile):
PASS P1 brakes: budget 1 executes one item, two stay resident; pause/snapshot/resume == uninterrupted — after call 1: records=1, resident=2; resumed in 2 more calls; 3 records in both reports; traces, cells, boundary digests equal
PASS P1b per-item vs time-slice: threshold crossing and failure isolation differ observably — same-time A(-20) then B(+20) on scarcity 25 with threshold 20: slice hunger=0 (net 0, no crossing), per-item hunger=20 (B crosses); one defective item: slice commits nothing, per-item commits the other two
PASS P2 report contract: exactly own records in order across pauses and in-memory restore; next request isolated — 4 calls; report 1 = 3 records at t=20 (2 committed, 1 rejected), report 2 = 1 record at t=30; undelivered empty; concatenation == trace. Restore is an in-memory clone: NO codec, NO crash-recovery or exactly-once claim
FAIL P3 batch digest distinguishes different child payloads under the same child key — same child WorkKey: true; batch digests differ: false; records differ: true; (engine digests differ: true — the scheduler commits to payload hashes regardless)
PASS P3b missing body / digest mismatch rejected, nothing written — missing body -> MissingBody: true; corrupted body -> BodyDigestMismatch with no write: true
PASS P4 commit-time collision = committed poison; report matches state; poisoned-only slice reported not executed — cell write committed (scarcity=7), contested key listed under conflicts and Conflicted in the scheduler; at t=15 the poisoned slot drains as a conflict record with no execution (drought stays 0)
PASS P4b overflow: delay u64::MAX-5 at now=10 -> TimeOverflow; occurrence u64::MAX -> OccurrenceOverflow; no partial writes — build profile: debug; TimeOverflow rejected cleanly: true; OccurrenceOverflow rejected cleanly: true
1 check(s) failed
