# S.P.A.R.K. Phase 1 — Claude Opus Re-Foundation Mission

## Mode

**PHASE-1 RE-FOUNDATION IMPLEMENTATION WRITER**

**Model: Opus**
**Effort: HIGH**

This is not another correction pass.

The previous Sonnet implementation and bounded correction failed to converge on three foundational defect classes. Preserve them as evidence and re-found the affected Phase-1 implementation against permanent adversarial acceptance tests.

## Autonomy

Work autonomously within scope.

You are pre-authorized to:

- inspect the repository and Git history;
- create a clean branch/worktree for the re-foundation;
- add permanent adversarial tests;
- rewrite/refactor affected Phase-1 Rust modules;
- remove superseded implementation inside the re-foundation branch where necessary;
- run Cargo/rustup local validation;
- use ordinary Git operations and coherent commits;
- create the final report and Downloads transfer copy.

Do not ask the operator for routine permission.

Stop only for:

- conflict with a frozen Phase-0 architecture;
- need for a new causal primitive;
- paid/cloud/credentialed resources;
- destructive action affecting unrelated data;
- irreconcilable repository conflict;
- Phase-2 scope expansion.

## Required reading

1. `engineering/phase0/CONTROLLING_BLUEPRINT_v0.2.md`
2. all accepted Phase-0 ADRs
3. `engineering/phase0/PHASE_0_CODEX_CLOSURE_REVIEW_2026-08-25.md`
4. `engineering/phase1/PHASE_1_IMPLEMENTATION_BRIEF.md`
5. `engineering/phase1/PHASE_1_CODEX_INDEPENDENT_REVIEW_2026-08-26.md`
6. `engineering/phase1/PHASE_1_CLAUDE_CORRECTION_REPORT_2026-08-26.md`
7. `engineering/phase1/PHASE_1_CODEX_CORRECTION_REREVIEW_2026-08-26.md`
8. `engineering/phase1/PHASE_1_CONVERGENCE_DECISION.md`
9. `engineering/phase1/PHASE_1_REFOUNDATION_BRIEF_v0.1.md`

Repository behavior and independent review evidence outrank prior writer claims.

## Mandatory workflow

### Step 1 — isolate

Create an isolated re-foundation branch/worktree from the current canonical repository state.

Do not rewrite the historical failed commits.

### Step 2 — encode failures first

Before implementing replacements, add permanent adversarial tests for every still-open finding in `PHASE_1_REFOUNDATION_BRIEF_v0.1.md`.

Demonstrate that the relevant tests fail against the inherited implementation.

Record this baseline in the final report.

### Step 3 — re-found affected modules

Rewrite the affected foundation boundaries.

Do not mechanically patch around the adversarial test names.

Fix the underlying properties:

- semantic/admission data separation;
- structured stage/finality evidence;
- trusted schema activation provenance;
- complete semantic scheduler key;
- validated unique-key config;
- bounded canonical construction and atomic overflow handling.

Preserve independently closed pieces only after inspecting them.

### Step 4 — full regression

Run all original and new tests.

Do not delete or weaken an architectural test merely because the new API is inconvenient.

### Step 5 — cross-target/static checks

Run installed Windows/Android target checks without operator prompting.

Do not claim platform execution.

## Scope

Still forbidden:

- Phase-2 propagation/effect runtime;
- service transport;
- persistence backend;
- actor behavior;
- MCI/game adapters;
- dialogue/voice;
- broad catalogs;
- scripting/plugin runtime;
- runtime LLM.

## Completion gate

Run:

```bash
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo metadata --format-version 1
```

The final working tree must be clean.

## Required report

Create canonical:

```text
engineering/phase1/PHASE_1_REFOUNDATION_IMPLEMENTATION_REPORT_2026-08-26.md
```

Copy identical file to:

```text
~/Downloads/PHASE_1_REFOUNDATION_IMPLEMENTATION_REPORT_2026-08-26.md
```

Report:

- branch/worktree;
- exact commits;
- failing baseline adversarial tests before rewrite;
- final test count/results;
- module/API replacement summary;
- every open prior finding disposition;
- closed-item regression results;
- Windows/Android static check results;
- any genuine conflict/deviation;
- `PHASE_2_AUTHORIZATION: NO`.

## Final operator response

Return only:

- `PHASE_1_REFOUNDATION_STATUS: COMPLETE` or `BLOCKED`
- branch/worktree
- commit hash(es)
- baseline adversarial failures encoded
- final test result/count
- fmt
- clippy
- Windows checks
- Android checks
- canonical report path
- Downloads report path
- genuine blocker/deviation
- `PHASE_2_AUTHORIZATION: NO`

Then stop.
