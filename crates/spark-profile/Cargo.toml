[package]
name = "spark-profile"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "S.P.A.R.K. profile manifest logical types, validation skeleton, and canonical content hashing/definition fingerprints."

[dependencies]
spark-core = { path = "../spark-core" }

[lints.rust]
unsafe_code = "forbid"
